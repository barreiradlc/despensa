import React from 'react';
import { ProvisionContainer, ProvisionListContainer, ProvisionTabLabel } from '../../components/styles/pantry';

const ProvisionFetchComponent: React.FC = () => {
    return (
        <ProvisionListContainer>

            <ProvisionContainer>
                <ProvisionTabLabel>

                </ProvisionTabLabel>
            </ProvisionContainer>

        </ProvisionListContainer>
    );
}

export default ProvisionFetchComponent;